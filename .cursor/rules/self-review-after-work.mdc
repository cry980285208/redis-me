---
description: 功能/改动完成后必须自行复核，再交用户验证
alwaysApply: true
---

# 做完必复核

完成一段可交付改动（功能阶段、修 bug、较大重构）后，在交给用户验证或提交前，**先自行复核一遍**，不要默认「写完即完」。

## 复核做什么

1. **对照需求/计划**：该阶段声称的能力是否都接上（含边界：空值、不支持版本报错、类型 Tag/简称等）。
2. **路径完整性**：读写/扫描/删改/复制命令等分支是否都覆盖新类型或新逻辑；有无漏改的 `match` / `supports*` / i18n。
3. **对照现有模式**：与邻近类型（如 List/Hash）行为是否一致；分页、精确查询、wire 编码等有无抄漏。
4. **静态验证**：能跑的 `cargo check` / 相关单测 / lint 尽量跑；明显类型与空值问题当场修。
5. **已知瑕疵**：复核中发现可立刻修的小问题直接修；若属下阶段范围，在回复里点明，勿 silently 留下明显缺陷。

## 回复用户时

用简短条目说明：已复核通过的点、已顺手修的点、留给用户手测的点（或明确无阻断问题）。

## 不必做

- 替用户做完整手工联调（用户说在验证时，以自检 + 静态检查为主）。
- 为「复核」单独开超大范围重构。
